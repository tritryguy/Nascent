#!/usr/bin/python3

# pipCount

print("\n\n\t\t\tzbar1\n\n")

principale = int(input("\t$$$$ >>>  "))
pips = int(input("\tpips >>>  "))

gains = principale * (pips / 1000)

print(gains)

# add "spread" and overhead charges
# pipeline for real time tracking
